2026-07-04 resume note:

- `omo ulw-loop status --json` could not run because `omo` is not on PATH in this shell.
- A cache search under `$HOME/.codex` found no `components/ulw-loop/dist/cli.js`.
- Existing durable state currently consists of `.omo/ulw-loop/brief-wegit-migration-2026-07-04.md`; no `goals.json` or `ledger.jsonl` was present in `.omo/ulw-loop` during resume.
- Continued from the active Codex goal and the migration evidence under `.omo/evidence/wegit-full-migration-2026-07-04/`.
