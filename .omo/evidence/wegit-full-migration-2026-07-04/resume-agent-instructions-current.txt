.claude\hooks\worktree.mts:40:	// Build the worktree path following GitLens conventions:
.claude\skills\add-ai-provider\SKILL.md:3:description: Add a new AI provider integration to GitLens
.claude\skills\add-ai-provider\SKILL.md:8:Add a new AI provider to GitLens with all required boilerplate.
.claude\skills\add-ai-provider\SKILL.md:21:4. **Requires account?** — Whether GitKraken account is needed vs user-provided API key
.agents\skills\a11y-audit\references\verification.md:97:> 1. Open the graph view in GitLens.
.claude\skills\dev-scope\SKILL.md:35:gh issue view <number> --repo gitkraken/vscode-gitlens --json number,title,body,comments,labels,state,author,createdAt,updatedAt,milestone,assignees
.claude\skills\investigate\SKILL.md:172:The repo slug comes from the decisions JSON's corresponding evidence pack, or default to `gitkraken/vscode-gitlens`.
.claude\skills\live-exercise\SKILL.md:87:- **Consistency** — patterns, terminology, icons, visual language match the rest of GitLens
.claude\skills\live-exercise\SKILL.md:202:- **Rationale**: GitLens convention is `gl-progress-spinner` on any action >500ms. Empty state had none.
.claude\skills\live-exercise\SKILL.md:383:Features gated by subscription (Commit Graph beyond local repos, Launchpad, Worktrees, Cloud Patches, Composer, all AI features, Drafts, Workspaces, etc.) won't unlock without a Paid/Trial session. Use the **subscription simulator** documented in `/live-inspect` (section: "Exercising Pro-gated features"). Pass `dismissOnboarding: true` on the start call to pre-dismiss every GitLens onboarding overlay — they intercept clicks during automation. State (subscription + onboarding flags) is restored when you call with `state: null`.
.agents\skills\add-icon\SKILL.md:3:description: Add new icons to the GitLens GL Icons font
.agents\skills\worktree\SKILL.md:3:description: Use when starting feature work that needs isolation from current workspace or before executing implementation plans - creates isolated git worktrees following GitLens conventions
.agents\skills\worktree\SKILL.md:6:# GitLens Worktree Creation
.claude\skills\a11y-flow-audit\references\wcag-criteria.md:89:- **VS Code webview calibration (and other embedded surfaces):** VS Code webviews do NOT own a traditional `<title>` in the user-visible sense — the containing panel/tab title is set by the extension host (e.g., `panel.title = 'GitLens Home'`), and the webview's `<title>` inside its HTML is not surfaced to the user. A spec-literal "add a `<title>` element" finding on a VS Code webview is correct-but-useless. The flow-audit equivalent is: the webview's root landmark (typically `<main>`) MUST expose the view's identity via an accessible name — `aria-label="GitLens Home"` or `aria-labelledby` pointing at a visible `<h1>`. Similar calibration applies to IDE tool panels, desktop app embedded panes, and any surface where the containing chrome owns the title: audit the root landmark's accessible name, not the HTML `<title>`.
.claude\skills\audit-commits\SKILL.md:61:- Fixes an issue where the _Home_ view would not update when switching repositories ([#4717](https://github.com/gitkraken/vscode-gitlens/issues/4717))
.agents\skills\audit-commits\SKILL.md:61:- Fixes an issue where the _Home_ view would not update when switching repositories ([#4717](https://github.com/gitkraken/vscode-gitlens/issues/4717))
.claude\skills\worktree\SKILL.md:3:description: Use when starting feature work that needs isolation from current workspace or before executing implementation plans - creates isolated git worktrees following GitLens conventions
.claude\skills\worktree\SKILL.md:6:# GitLens Worktree Creation
.claude\skills\create-issue\SKILL.md:22:3. **Check duplicates**: `gh search issues --repo gitkraken/vscode-gitlens "<keywords>"`
.agents\skills\add-test\SKILL.md:96:		await vscode.gitlens.openGitLensSidebar();
.agents\skills\add-test\SKILL.md:135:Use `getGitLensWebview(title, purpose)` to get a `FrameLocator` for webview content:
.agents\skills\add-test\SKILL.md:138:const webview = await vscode.gitlens.getGitLensWebview('Home', 'webviewView');
.claude\skills\a11y-remediate\references\staffing-translation.md:104:> The component audited ships in `@gitkraken/gitkraken-components` (consumed by `vscode-gitlens` and the GitKraken desktop app). The engineer-day estimate above is library-side work only. Downstream regression testing in consumer products adds approximately 20–40% per consumer and is owned by those product teams.
.claude\skills\a11y-flow-audit\references\output-format.md:43:- **(a) Phrase in terms of the surface:** render the sentence in surface language — e.g., "When you open the GitLens Home view, you'll see…" or "Inside the Launchpad panel, you'll notice…". Use this when the surface has a recognizable user-facing name.
.claude\skills\prioritize\SKILL.md:48:gh search issues --repo gitkraken/vscode-gitlens "<significant keywords from title>" --state open --json number --limit 20
.claude\skills\prioritize\SKILL.md:73:1. **Won't Fix** — Issue is out of scope for GitLens OR describes behavior that is working as designed. Requires clear evidence.
.claude\skills\prioritize\SKILL.md:109:### [#NNNN — Title](https://github.com/gitkraken/vscode-gitlens/issues/NNNN)
.claude\skills\commit\SKILL.md:3:description: Create well-formatted git commits following GitLens conventions
.claude\skills\commit\SKILL.md:8:Create a well-formatted git commit following GitLens conventions.
.agents\skills\add-command\SKILL.md:68:    "category": "GitLens",
.claude\skills\triage\SKILL.md:51:Is the body empty, incoherent, or unrelated to GitLens?
.claude\skills\triage\SKILL.md:147:- (b) The reported GitLens version is significantly outdated
.claude\skills\ux-review\SKILL.md:106:Does it feel like GitLens?
.claude\skills\ux-review\SKILL.md:108:- **Interaction patterns**: Does this feature follow the same patterns as similar existing features? If GitLens already has a way to do something analogous, this should work the same way unless there's a clear reason not to.
.claude\skills\ux-review\SKILL.md:109:- **Terminology**: Does it use the same words GitLens uses elsewhere for the same concepts? (e.g., "repository" vs "repo", "stash" vs "shelf")
.claude\skills\ux-review\SKILL.md:110:- **Visual language**: Icons, tree item structures, webview layouts, quick pick formats -- do they match existing GitLens conventions?
.claude\skills\ux-review\SKILL.md:111:- **Behavior expectations**: If the user has learned how one GitLens feature works, does that knowledge transfer to this one?
.claude\skills\ux-review\SKILL.md:119:- **Composability**: Does the feature work well alongside other VS Code features and other GitLens features? Does it conflict with common keybindings, monopolize panels, or break expected multi-repo behavior?
.claude\skills\update-issues\SKILL.md:73:gh issue view <number> --repo gitkraken/vscode-gitlens --json state,labels,milestone
.claude\skills\update-issues\SKILL.md:118:gh issue edit <number> --repo gitkraken/vscode-gitlens --add-label "<label>"
.claude\skills\update-issues\SKILL.md:121:gh issue edit <number> --repo gitkraken/vscode-gitlens --remove-label "<label>"
.claude\skills\update-issues\SKILL.md:124:gh issue edit <number> --repo gitkraken/vscode-gitlens --milestone "<milestone>"
.claude\skills\update-issues\SKILL.md:127:gh issue comment <number> --repo gitkraken/vscode-gitlens --body "<message>"
.claude\skills\update-issues\SKILL.md:130:gh issue close <number> --repo gitkraken/vscode-gitlens --reason "not planned" --comment "<message>"
.claude\skills\update-issues\SKILL.md:133:gh issue close <number> --repo gitkraken/vscode-gitlens --reason "completed" --comment "<message>"
.agents\skills\ux-review\SKILL.md:106:Does it feel like GitLens?
.agents\skills\ux-review\SKILL.md:108:- **Interaction patterns**: Does this feature follow the same patterns as similar existing features? If GitLens already has a way to do something analogous, this should work the same way unless there's a clear reason not to.
.agents\skills\ux-review\SKILL.md:109:- **Terminology**: Does it use the same words GitLens uses elsewhere for the same concepts? (e.g., "repository" vs "repo", "stash" vs "shelf")
.agents\skills\ux-review\SKILL.md:110:- **Visual language**: Icons, tree item structures, webview layouts, quick pick formats -- do they match existing GitLens conventions?
.agents\skills\ux-review\SKILL.md:111:- **Behavior expectations**: If the user has learned how one GitLens feature works, does that knowledge transfer to this one?
.agents\skills\ux-review\SKILL.md:119:- **Composability**: Does the feature work well alongside other VS Code features and other GitLens features? Does it conflict with common keybindings, monopolize panels, or break expected multi-repo behavior?
.claude\skills\add-icon\SKILL.md:3:description: Add new icons to the GitLens GL Icons font
.claude\skills\add-command\SKILL.md:68:    "category": "GitLens",
.agents\skills\live-inspect\SKILL.md:3:description: Use for one-off / single-question inspection of the running GitLens extension — examining UI state, reading logs, checking feature flags, dispatching a command, or asking "what does the live DOM look like right now". Reference for `vscode-inspector` MCP primitives. For iterative debug-and-fix loops on UI bugs (sweep → fix → re-verify), use `/live-exercise` instead.
.agents\skills\live-inspect\SKILL.md:8:Launch a real VS Code instance with GitLens loaded, then inspect UI elements, read logs, interact with views, and evaluate runtime values — all programmatically via Playwright.
.agents\skills\live-inspect\SKILL.md:18:| `launch`              | Start VS Code with GitLens loaded (persistent session)                               |
.agents\skills\live-inspect\SKILL.md:66:Every GitLens webview has a `gitlens.views.<name>.refresh` command (e.g. `gitlens.views.welcome.refresh`, `gitlens.views.graph.refresh`, `gitlens.views.commitDetails.refresh`). These fully reload the webview with fresh JS/CSS.
.agents\skills\live-inspect\SKILL.md:223:### GitLens Webview Reference
.agents\skills\live-inspect\SKILL.md:237:| `gitlens.showSettingsPage`      | GitLens Settings     | `gl-settings-app`       | `gitlens.settings.refresh`            |
.agents\skills\live-inspect\SKILL.md:243:GitLens webviews use **Lit web components** with Shadow DOM. Here's the recommended approach:
.agents\skills\live-inspect\SKILL.md:296:| `--activation-wait <ms>`      | Wait time for GitLens activation (default 8000)  |
.agents\skills\live-inspect\SKILL.md:325:Returns `true` on start, `false` on stop. Pass `dismissOnboarding: true` to also pre-dismiss every GitLens onboarding tour/banner (composer welcome, home walkthrough, MCP banner, rebase-editor warning, integration banner, SCM-grouped welcome) — they're full-screen overlays that intercept clicks during automation. State is restored on stop.
.agents\skills\live-inspect\SKILL.md:382:GitLens AI features (Generate Commit Message, Explain \*, Generate Changelog, Composer, Review Changes, Generate Search Query) cannot use real provider calls during automated inspection — they cost money, require keys, and produce non-deterministic outputs you can't assert against. The extension ships a **deterministic AI simulator** in DEBUG builds that you control via VS Code commands.
.claude\skills\add-test\SKILL.md:96:		await vscode.gitlens.openGitLensSidebar();
.claude\skills\add-test\SKILL.md:135:Use `getGitLensWebview(title, purpose)` to get a `FrameLocator` for webview content:
.claude\skills\add-test\SKILL.md:138:const webview = await vscode.gitlens.getGitLensWebview('Home', 'webviewView');
.agents\skills\add-ai-provider\SKILL.md:3:description: Add a new AI provider integration to GitLens
.agents\skills\add-ai-provider\SKILL.md:8:Add a new AI provider to GitLens with all required boilerplate.
.agents\skills\add-ai-provider\SKILL.md:21:4. **Requires account?** — Whether GitKraken account is needed vs user-provided API key
.claude\skills\review\SKILL.md:3:description: Code review against GitLens standards with optional impact completeness audit
.claude\skills\review\SKILL.md:8:Review code changes against GitLens coding standards and verify change completeness.
.agents\skills\update-issues\SKILL.md:73:gh issue view <number> --repo gitkraken/vscode-gitlens --json state,labels,milestone
.agents\skills\update-issues\SKILL.md:118:gh issue edit <number> --repo gitkraken/vscode-gitlens --add-label "<label>"
.agents\skills\update-issues\SKILL.md:121:gh issue edit <number> --repo gitkraken/vscode-gitlens --remove-label "<label>"
.agents\skills\update-issues\SKILL.md:124:gh issue edit <number> --repo gitkraken/vscode-gitlens --milestone "<milestone>"
.agents\skills\update-issues\SKILL.md:127:gh issue comment <number> --repo gitkraken/vscode-gitlens --body "<message>"
.agents\skills\update-issues\SKILL.md:130:gh issue close <number> --repo gitkraken/vscode-gitlens --reason "not planned" --comment "<message>"
.agents\skills\update-issues\SKILL.md:133:gh issue close <number> --repo gitkraken/vscode-gitlens --reason "completed" --comment "<message>"
.claude\skills\live-inspect\SKILL.md:3:description: Use for one-off / single-question inspection of the running GitLens extension — examining UI state, reading logs, checking feature flags, dispatching a command, or asking "what does the live DOM look like right now". Reference for `vscode-inspector` MCP primitives. For iterative debug-and-fix loops on UI bugs (sweep → fix → re-verify), use `/live-exercise` instead.
.claude\skills\live-inspect\SKILL.md:8:Launch a real VS Code instance with GitLens loaded, then inspect UI elements, read logs, interact with views, and evaluate runtime values — all programmatically via Playwright.
.claude\skills\live-inspect\SKILL.md:18:| `launch`              | Start VS Code with GitLens loaded (persistent session)                               |
.claude\skills\live-inspect\SKILL.md:39:- **`read_logs` uses `pattern`, NOT `filter`.** A wrong/unknown arg is silently ignored and it falls back to `pattern: "GitLens"` — so you only ever see the activation banner and wrongly conclude "no logs". Always: `read_logs({ pattern: "<tag>", last_n })`.
.claude\skills\live-inspect\SKILL.md:40:- `read_logs` DOES capture extension-**host** `console.log/warn/error` and GitLens `Logger.warn`/`info`/`error` (info+). It reads the GitLens **LogOutputChannel**, also on disk at `.vscode-test/user-data/logs/<TS>/window1/exthost/eamodio.gitlens/GitLens.log`. `read_console` is webview-only (does not see host logs).
.claude\skills\live-inspect\SKILL.md:41:- **`@debug`/`@trace` decorator logs are filtered out.** The channel defaults to `info`; GitLens' rich tracing is `debug`/`trace`. Console-mirroring (`Logger.isDebugging`) is gated on `ExtensionMode.Development`, but the inspector runs in **Test** mode → off. `gitlens.enableDebugLogging` (which runs `workbench.action.output.activeOutputLogLevel.debug`) does NOT take headless. So for ad-hoc host tracing, **instrument with `Logger.warn('[tag] …')`** (info+, always written) and read via `read_logs({ pattern: "[tag]" })`. `gitlens.outputLevel` is deprecated — don't rely on it.
.claude\skills\live-inspect\SKILL.md:72:Every GitLens webview has a `gitlens.views.<name>.refresh` command (e.g. `gitlens.views.welcome.refresh`, `gitlens.views.graph.refresh`, `gitlens.views.commitDetails.refresh`). These fully reload the webview with fresh JS/CSS.
.claude\skills\live-inspect\SKILL.md:229:### GitLens Webview Reference
.claude\skills\live-inspect\SKILL.md:243:| `gitlens.showSettingsPage`      | GitLens Settings     | `gl-settings-app`       | `gitlens.settings.refresh`            |
.claude\skills\live-inspect\SKILL.md:249:GitLens webviews use **Lit web components** with Shadow DOM. Here's the recommended approach:
.claude\skills\live-inspect\SKILL.md:302:| `--activation-wait <ms>`      | Wait time for GitLens activation (default 8000)  |
.claude\skills\live-inspect\SKILL.md:331:Returns `true` on start, `false` on stop. Pass `dismissOnboarding: true` to also pre-dismiss every GitLens onboarding tour/banner (composer welcome, home walkthrough, MCP banner, rebase-editor warning, integration banner, SCM-grouped welcome) — they're full-screen overlays that intercept clicks during automation. State is restored on stop.
.claude\skills\live-inspect\SKILL.md:388:GitLens AI features (Generate Commit Message, Explain \*, Generate Changelog, Composer, Review Changes, Generate Search Query) cannot use real provider calls during automated inspection — they cost money, require keys, and produce non-deterministic outputs you can't assert against. The extension ships a **deterministic AI simulator** in DEBUG builds that you control via VS Code commands.
.agents\skills\live-exercise\SKILL.md:87:- **Consistency** — patterns, terminology, icons, visual language match the rest of GitLens
.agents\skills\live-exercise\SKILL.md:202:- **Rationale**: GitLens convention is `gl-progress-spinner` on any action >500ms. Empty state had none.
.agents\skills\live-exercise\SKILL.md:383:Features gated by subscription (Commit Graph beyond local repos, Launchpad, Worktrees, Cloud Patches, Composer, all AI features, Drafts, Workspaces, etc.) won't unlock without a Paid/Trial session. Use the **subscription simulator** documented in `/live-inspect` (section: "Exercising Pro-gated features"). Pass `dismissOnboarding: true` on the start call to pre-dismiss every GitLens onboarding overlay — they intercept clicks during automation. State (subscription + onboarding flags) is restored when you call with `state: null`.
.claude\skills\live-perf\SKILL.md:8:Exercise the feature to measure it, not to change it. Capture baselines. Evaluate measurements and audit GitLens conventions. Dispatch fix agents under three-tier discipline — never on speculation. Re-measure to confirm. Loop until no measured regressions and no convention violations remain.
.claude\skills\live-perf\SKILL.md:108:> **Multi-webview perf**: When two GitLens webviews are open at once (e.g. measuring Commit Details while the Graph is also visible), `evaluate_in_webview` without targeting silently runs in the first webview — usually the wrong one. Run `list_webviews` first, then pass `webview_url: "commitDetails"` (URL substring) or `webview_index: <n>` to scope every measurement to the right webview. Same params work on `wait_for_webview`, `inspect_dom`, `aria_snapshot`, `screenshot`, `click`.
.claude\skills\live-perf\SKILL.md:142:Tools: `read_logs` with pattern matching on `git ` command logs (GitLens logs git calls with debug logging). Code audit for patterns in `src/env/node/git/sub-providers/`. `evaluate` on the extension host to count command invocations over a user-action window.
.claude\skills\live-perf\SKILL.md:151:| **Conventions** | Violation of a GitLens convention (missed cache/memoize on hot getter, serialized awaits, missing debounce) | **Dispatch liberty-fix agent.** Log entry in `decisions.md`. No baseline needed — convention IS the bug. | `PC`      |
.agents\skills\dev-scope\SKILL.md:35:gh issue view <number> --repo gitkraken/vscode-gitlens --json number,title,body,comments,labels,state,author,createdAt,updatedAt,milestone,assignees
.agents\skills\commit\SKILL.md:3:description: Create well-formatted git commits following GitLens conventions
.agents\skills\commit\SKILL.md:8:Create a well-formatted git commit following GitLens conventions.
.agents\skills\create-issue\SKILL.md:22:3. **Check duplicates**: `gh search issues --repo gitkraken/vscode-gitlens "<keywords>"`
.agents\skills\triage\SKILL.md:51:Is the body empty, incoherent, or unrelated to GitLens?
.agents\skills\triage\SKILL.md:147:- (b) The reported GitLens version is significantly outdated
.agents\skills\investigate\SKILL.md:172:The repo slug comes from the decisions JSON's corresponding evidence pack, or default to `gitkraken/vscode-gitlens`.
.agents\skills\prioritize\SKILL.md:48:gh search issues --repo gitkraken/vscode-gitlens "<significant keywords from title>" --state open --json number --limit 20
.agents\skills\prioritize\SKILL.md:73:1. **Won't Fix** — Issue is out of scope for GitLens OR describes behavior that is working as designed. Requires clear evidence.
.agents\skills\prioritize\SKILL.md:109:### [#NNNN — Title](https://github.com/gitkraken/vscode-gitlens/issues/NNNN)
.agents\skills\live-perf\SKILL.md:8:Exercise the feature to measure it, not to change it. Capture baselines. Evaluate measurements and audit GitLens conventions. Dispatch fix agents under three-tier discipline — never on speculation. Re-measure to confirm. Loop until no measured regressions and no convention violations remain.
.agents\skills\live-perf\SKILL.md:108:> **Multi-webview perf**: When two GitLens webviews are open at once (e.g. measuring Commit Details while the Graph is also visible), `evaluate_in_webview` without targeting silently runs in the first webview — usually the wrong one. Run `list_webviews` first, then pass `webview_url: "commitDetails"` (URL substring) or `webview_index: <n>` to scope every measurement to the right webview. Same params work on `wait_for_webview`, `inspect_dom`, `aria_snapshot`, `screenshot`, `click`.
.agents\skills\live-perf\SKILL.md:142:Tools: `read_logs` with pattern matching on `git ` command logs (GitLens logs git calls with debug logging). Code audit for patterns in `src/env/node/git/sub-providers/`. `evaluate` on the extension host to count command invocations over a user-action window.
.agents\skills\live-perf\SKILL.md:151:| **Conventions** | Violation of a GitLens convention (missed cache/memoize on hot getter, serialized awaits, missing debounce) | **Dispatch liberty-fix agent.** Log entry in `decisions.md`. No baseline needed — convention IS the bug. | `PC`      |
.agents\skills\review\SKILL.md:3:description: Code review against GitLens standards with optional impact completeness audit
.agents\skills\review\SKILL.md:8:Review code changes against GitLens coding standards and verify change completeness.
.claude\skills\a11y-audit\references\verification.md:97:> 1. Open the graph view in GitLens.
.agents\skills\a11y-remediate\references\staffing-translation.md:104:> The component audited ships in `@gitkraken/gitkraken-components` (consumed by `vscode-gitlens` and the GitKraken desktop app). The engineer-day estimate above is library-side work only. Downstream regression testing in consumer products adds approximately 20–40% per consumer and is owned by those product teams.
.agents\skills\a11y-flow-audit\references\output-format.md:43:- **(a) Phrase in terms of the surface:** render the sentence in surface language — e.g., "When you open the GitLens Home view, you'll see…" or "Inside the Launchpad panel, you'll notice…". Use this when the surface has a recognizable user-facing name.
.agents\skills\a11y-flow-audit\references\wcag-criteria.md:89:- **VS Code webview calibration (and other embedded surfaces):** VS Code webviews do NOT own a traditional `<title>` in the user-visible sense — the containing panel/tab title is set by the extension host (e.g., `panel.title = 'GitLens Home'`), and the webview's `<title>` inside its HTML is not surfaced to the user. A spec-literal "add a `<title>` element" finding on a VS Code webview is correct-but-useless. The flow-audit equivalent is: the webview's root landmark (typically `<main>`) MUST expose the view's identity via an accessible name — `aria-label="GitLens Home"` or `aria-labelledby` pointing at a visible `<h1>`. Similar calibration applies to IDE tool panels, desktop app embedded panes, and any surface where the containing chrome owns the title: audit the root landmark's accessible name, not the HTML `<title>`.
